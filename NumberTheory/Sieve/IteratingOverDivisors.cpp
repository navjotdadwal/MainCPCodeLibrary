// int divisors[n + 1];
// for (int i = 1; i <= n; ++i)
//  for (int j = i; j <= n; j += i)
//   ++divisors[j];

void precompute()
{
  for (int i = 2; i < N; i++) {
    for (int j = i + i; j < N; j += i){
        
    }
  }  
}
